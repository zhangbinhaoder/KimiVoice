# KimiVoice 配置示例

## 📋 配置清单

在开始使用前，请准备以下信息：

- [ ] Moonshot API Key
- [ ] Android设备（已授予必要权限）
- [ ] 安静的测试环境
- [ ] 系统签名文件（platform.jks）

---

## 🔑 1. 获取 Moonshot API Key

### 步骤一：注册账号

1. 访问 Moonshot AI 官网
   ```
   https://platform.moonshot.cn/
   ```

2. 点击右上角"注册/登录"

3. 使用手机号或邮箱完成注册

### 步骤二：创建API Key

1. 登录后进入控制台
   ```
   https://platform.moonshot.cn/console
   ```

2. 进入"API密钥"页面

3. 点击"创建新的密钥"

4. 为密钥命名（如：KimiVoice-Production）

5. **复制并保存密钥**（仅显示一次！）
   ```
   示例格式：sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```

### 步骤三：充值额度（可选）

1. 新用户通常有试用额度

2. 如需长期使用，进入"账户充值"

3. 最低充值金额通常为 10-50 元

---

## 🎚️ 2. 音频能量阈值配置

### 推荐配置

| 环境类型 | 阈值范围 | 说明 |
|---------|---------|------|
| **安静室内** | 1500 - 2500 | 家庭、办公室晚上 |
| **普通环境** | 2500 - 3500 | 日常办公室、商店 |
| **嘈杂环境** | 3500 - 5000 | 街道、餐厅、车内 |
| **极端环境** | 5000+ | 工厂、施工现场 |

### 测试方法

1. **初始设置**
   ```
   默认值：2000
   ```

2. **逐步调整**
   ```
   太敏感（误触发）→ 增加500
   不够灵敏（不触发）→ 减少500
   ```

3. **最佳实践**
   ```
   在实际使用环境中测试3-5次
   找到最稳定的阈值
   ```

### 配置示例

**场景1：家庭使用（夜晚）**
```
环境：安静，偶有电器声音
推荐阈值：1800
预期效果：清晰说话即可触发，背景噪音不会误触发
```

**场景2：办公室使用（工作时间）**
```
环境：键盘声、同事交谈
推荐阈值：3000
预期效果：需要稍大声说话，避免误识别周围对话
```

**场景3：车载使用**
```
环境：引擎声、风噪、路噪
推荐阈值：4500
预期效果：需要清晰大声说话，抗干扰强
```

---

## 📱 3. 应用配置界面说明

### 界面布局

```
┌─────────────────────────────┐
│   KimiVoice                 │
├─────────────────────────────┤
│                             │
│  [ API Key输入框 ]          │  ← 输入Moonshot API Key
│  提示：请输入Moonshot API Key│
│                             │
│  [ 能量阈值输入框 ]          │  ← 输入能量阈值（如：2000）
│  提示：音频能量阈值（默认2000）│
│                             │
│  [ 保存配置 ] 按钮           │  ← 点击保存
│                             │
│  ──────────────────────────  │
│                             │
│  [ 启动监听服务 ]  [ 停止监听 ]│  ← 服务控制
│                             │
└─────────────────────────────┘
```

### 配置步骤

**步骤1：输入API Key**
```
1. 点击第一个输入框
2. 粘贴您的API Key
   示例：sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
3. 输入框会隐藏显示（密码模式）
```

**步骤2：设置阈值**
```
1. 点击第二个输入框
2. 输入数值（建议2000-5000）
3. 当前显示的是上次保存的值
```

**步骤3：保存配置**
```
1. 点击"保存配置"按钮
2. 看到提示：
   - "API Key已加密保存"
   - "能量阈值已设为：XXXX"
3. API Key输入框会自动清空（安全考虑）
4. 提示会变为"API Key已配置"
```

---

## 🧪 4. 测试配置

### 测试准备

1. **环境检查**
   ```
   □ 确保环境相对安静
   □ 关闭其他音乐/视频应用
   □ 确保设备麦克风正常工作
   □ 确保网络连接正常
   ```

2. **权限检查**
   ```
   □ 录音权限已授予
   □ 通知权限已授予（Android 13+）
   □ 网络权限已授予
   ```

### 测试流程

**测试1：基础功能测试**
```
1. 点击"启动监听服务"
2. 观察通知栏显示"正在监听..."
3. 说话："你好"
4. 观察通知变为"正在记录..."
5. 停止说话2-3秒
6. 观察通知变为"处理中..."
7. 等待处理完成（3-10秒）
8. 回到"正在监听..."
9. 查看Logcat确认转写结果
```

**测试2：指令执行测试**
```
测试指令：
1. "打开WiFi" → 观察WiFi是否开启
2. "关闭WiFi" → 观察WiFi是否关闭
3. "静音" → 观察音量是否为0
4. "取消静音" → 观察音量是否恢复

预期结果：
- 每个指令都应该正确执行
- Logcat显示完整的处理流程
- 没有错误提示
```

**测试3：边界情况测试**
```
1. 极短语音（< 0.5秒）
   预期：被忽略，不处理
   
2. 长时间说话（> 10秒）
   预期：正常录制和处理
   
3. 连续多次说话
   预期：每次都能正确识别
   
4. 环境噪音
   预期：不会误触发
```

---

## 📊 5. 配置优化建议

### 性能优化

**降低延迟：**
```kotlin
// 在 ListenService.kt 中调整
private val maxSilenceFrames = 10  // 默认15，减小可快速结束录音
```

**提高准确度：**
```kotlin
// 在 ListenService.kt 中调整
private val minSpeechFrames = 8   // 默认5，增大可减少误触发
```

### 网络优化

**修改超时时间：**
```kotlin
// 在 KimiConstants.kt 中调整
const val CONNECT_TIMEOUT = 5L    // 默认10秒
const val READ_TIMEOUT = 10L      // 默认15秒
```

**修改重试次数：**
```kotlin
// 在 KimiConstants.kt 中调整
const val MAX_RETRY_COUNT = 3     // 默认2次
```

### 安全优化

**更改加密密钥：**
```kotlin
// 在 SecurityUtils.kt 中（首次使用前修改）
private const val AES_FALLBACK_KEY = "your_custom_key_16b"  // 必须16字节
```

**添加自定义指令：**
```kotlin
// 在 KimiConstants.kt 中
val ALLOWED_COMMANDS = listOf(
    "svc wifi",
    "settings put",
    // 添加您的自定义指令前缀
    "your_custom_cmd"
)
```

---

## 🔍 6. 日志分析

### 正常流程日志

```
D/KimiVoice: ListenService创建
D/KimiVoice: 配置加载成功: threshold=2000
D/KimiVoice: 录音已启动，缓冲区大小: 8192
D/KimiVoice: 检测到语音，开始记录
D/KimiVoice: 语音结束，开始处理
D/KimiVoice: 录音时长: 2.34秒
D/KimiVoice: WAV文件保存成功: 74880 字节
D/KimiVoice: 开始语音转文字
D/KimiVoice: 转写成功: 打开WiFi
D/KimiVoice: 发送AI对话: 打开WiFi
D/KimiVoice: AI生成指令: svc wifi enable
D/KimiVoice: 执行指令: svc wifi enable
D/KimiVoice: 指令执行完成，退出码: 0
```

### 常见错误日志

**错误1：API Key无效**
```
E/KimiVoice: 转写失败: Invalid API key
解决方案：检查API Key是否正确，是否已过期
```

**错误2：网络错误**
```
E/KimiVoice: 网络请求失败: Unable to resolve host
解决方案：检查网络连接，确保可以访问外网
```

**错误3：权限错误**
```
E/KimiVoice: 录音启动失败: Permission denied
解决方案：授予应用录音权限
```

**错误4：指令被拦截**
```
W/KimiVoice: 指令未通过安全验证: rm -rf /
解决方案：这是正常的安全保护，危险指令被拦截
```

---

## 💡 7. 实用技巧

### 技巧1：快速切换配置

创建多个配置文件：
```
家庭配置：阈值 1800
办公室配置：阈值 3000
车载配置：阈值 4500
```

### 技巧2：自定义唤醒流程

编辑提示词（在 NetworkUtils.kt）：
```kotlin
systemPrompt = """
你是智能助手，将用户语音转为shell命令。
规则：
1. 只返回命令，不要解释
2. WiFi操作用 svc wifi enable/disable
3. 音量用 settings put system volume_music X
4. 其他操作用对应的 shell 命令
"""
```

### 技巧3：添加常用指令映射

创建指令映射表：
```
用户说：打开微信 → am start com.tencent.mm
用户说：返回桌面 → input keyevent 3
用户说：打开相机 → am start -a android.media.action.STILL_IMAGE_CAMERA
```

---

## ✅ 配置完成检查清单

使用前请确认以下各项：

- [ ] API Key已正确配置并测试
- [ ] 能量阈值已根据环境调整
- [ ] 所有必要权限已授予
- [ ] 已完成基础功能测试
- [ ] 日志显示正常
- [ ] 网络连接正常
- [ ] 系统签名已正确应用
- [ ] 了解常用指令格式

---

**配置完成后，您就可以开始使用 KimiVoice 了！** 🎉
