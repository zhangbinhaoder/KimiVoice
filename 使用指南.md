# KimiVoice 使用指南

## 📦 构建完成

✅ **已成功生成两个版本的APK：**

| 版本 | 文件名 | 大小 | 说明 |
|------|--------|------|------|
| **Debug** | `app-debug.apk` | 6.49 MB | 开发调试版本，包含完整日志 |
| **Release** | `app-release.apk` | 5.19 MB | 正式版本，已混淆优化，使用系统签名 |

**APK位置：**
- Debug: `app\build\outputs\apk\debug\app-debug.apk`
- Release: `app\build\outputs\apk\release\app-release.apk`

---

## 🚀 快速开始

### 1️⃣ 安装应用

**方法一：ADB安装**
```bash
# 安装Debug版本
adb install app\build\outputs\apk\debug\app-debug.apk

# 安装Release版本（推荐）
adb install app\build\outputs\apk\release\app-release.apk
```

**方法二：直接推送到设备**
```bash
# 将APK复制到设备
adb push app\build\outputs\apk\release\app-release.apk /sdcard/

# 然后在设备上安装
adb shell pm install /sdcard/app-release.apk
```

---

## ⚙️ 配置步骤

### 2️⃣ 配置API Key

1. **打开应用**
   - 在Android设备上启动 KimiVoice 应用

2. **输入Moonshot API Key**
   - 在第一个输入框中输入您的 Moonshot API Key
   - 格式示例：`sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
   - API Key最少20个字符

3. **点击"保存配置"按钮**
   - API Key会使用 AES-GCM 加密存储在设备上
   - 系统会显示"API Key已加密保存"提示

**获取API Key：**
- 访问 Moonshot AI 官网：https://platform.moonshot.cn/
- 注册并登录账号
- 在API密钥页面创建新的API Key

---

### 3️⃣ 调整音频能量阈值

1. **在第二个输入框输入阈值**
   - **推荐范围**：2000 - 5000
   - **默认值**：2000
   - **环境建议**：
     - 安静环境：1500 - 2500
     - 普通环境：2500 - 3500
     - 嘈杂环境：3500 - 5000

2. **点击"保存配置"按钮**
   - 系统会显示"能量阈值已设为：XXXX"

**阈值说明：**
- 数值越低：越容易触发录音，但可能误触发
- 数值越高：需要更大声音才触发，但更稳定
- 建议先用默认值测试，再根据实际情况调整

---

## 🎤 测试功能

### 4️⃣ 授予权限

首次启动时会请求以下权限：

- ✅ **录音权限**：用于采集语音
- ✅ **通知权限**（Android 13+）：显示服务状态
- ✅ **网络权限**：调用API进行语音识别

**如果权限被拒绝：**
- 进入系统设置 → 应用管理 → KimiVoice
- 手动授予所有权限

---

### 5️⃣ 启动服务

1. **点击"启动监听服务"按钮**
   - 系统会显示"语音监听服务已启动"
   - 通知栏会显示"KimiVoice - 正在监听..."

2. **服务状态说明**
   ```
   初始化中...      → 服务正在启动
   正在监听...      → 等待语音输入
   正在记录...      → 检测到说话，开始录音
   处理中...        → 录音结束，正在处理
   错误：XXX       → 发生错误，查看提示
   ```

---

### 6️⃣ 说话测试

**测试流程：**

1. **保持设备在安静环境**
   - 确保没有持续的背景噪音

2. **清晰说出指令**
   ```
   示例指令：
   - "打开WiFi"
   - "关闭WiFi"
   - "静音"
   - "取消静音"
   - "截图"
   ```

3. **观察流程**
   ```
   您说话 
     ↓
   VAD检测到语音（通知显示"正在记录..."）
     ↓
   连续5帧语音后开始录音
     ↓
   连续15帧静音后结束录音（通知显示"处理中..."）
     ↓
   语音转文字（调用Moonshot API）
     ↓
   AI生成指令（调用Moonshot Chat API）
     ↓
   安全验证（白名单+黑名单检查）
     ↓
   执行指令
     ↓
   回到监听状态（通知显示"正在监听..."）
   ```

4. **录音长度建议**
   - 最短：0.5秒（太短会被忽略）
   - 最长：无限制（但建议10秒内）
   - 最佳：2-5秒的清晰指令

---

### 7️⃣ 查看日志

**方法一：Logcat实时查看**
```bash
# 查看所有日志
adb logcat | grep KimiVoice

# 只看错误和警告
adb logcat | grep -E "(ERROR|WARN)" | grep KimiVoice

# 使用Timber标签过滤
adb logcat -s "KimiVoice"
```

**方法二：查看崩溃日志**
```bash
# 日志保存在设备中
adb shell ls /sdcard/Android/data/com.example.kimivoice/files/crash/

# 拉取崩溃日志
adb pull /sdcard/Android/data/com.example.kimivoice/files/crash/ ./crash_logs/
```

**关键日志标识：**
```
[ListenService] 服务启动/停止
[AudioUtils] 音频处理相关
[NetworkUtils] API请求相关
[SecurityUtils] 加密解密相关
[CmdReceiver] 指令执行相关
```

---

### 8️⃣ 停止服务

- 点击"停止监听服务"按钮
- 系统会显示"语音监听服务已停止"
- 通知会消失

---

## 🔧 高级配置

### 修改VAD参数

如需调整语音检测灵敏度，编辑 `ListenService.kt`：

```kotlin
// 静音帧数阈值（默认15帧）
private val maxSilenceFrames = 15  // 减小更灵敏，增大更稳定

// 语音帧数阈值（默认5帧）
private val minSpeechFrames = 5   // 减小更快触发，增大防误触
```

### 修改允许的指令

编辑 `KimiConstants.kt` 中的白名单：

```kotlin
val ALLOWED_COMMANDS = listOf(
    "svc wifi",           // WiFi控制
    "settings put",       // 系统设置
    "am start",           // 启动应用
    "am broadcast",       // 发送广播
    "service call",       // 服务调用
    "input tap",          // 模拟点击
    "input swipe"         // 模拟滑动
    // 添加更多允许的指令前缀
)
```

---

## 📱 支持的指令示例

### WiFi控制
```
用户说：打开WiFi
AI生成：svc wifi enable

用户说：关闭WiFi
AI生成：svc wifi disable
```

### 音量控制
```
用户说：静音
AI生成：settings put system volume_music 0

用户说：音量50%
AI生成：settings put system volume_music 8
```

### 屏幕控制
```
用户说：截图
AI生成：input keyevent 120

用户说：锁屏
AI生成：input keyevent 26
```

---

## ⚠️ 注意事项

### 1. 系统权限
- 本应用使用**系统签名**（android.uid.system）
- 需要在系统级ROM中才能使用完整功能
- 普通签名版本功能会受限

### 2. API使用
- Moonshot API需要付费或试用额度
- 语音转文字：按分钟计费
- AI对话：按Token计费
- 建议设置API使用限额

### 3. 隐私安全
- API Key使用 AES-GCM 加密存储
- Android 6.0+ 使用 KeyStore 硬件级保护
- 音频文件存储在应用私有目录
- 处理完成后自动清理

### 4. 性能优化
- 服务运行在独立进程（:listen_service）
- 使用Kotlin协程避免阻塞
- 自动清理音频缓存
- 内存占用约 30-50 MB

---

## 🐛 常见问题

### Q1: 服务启动失败
**原因：** API Key未配置或格式错误
**解决：** 重新输入有效的API Key（至少20字符）

### Q2: 无法录音
**原因：** 缺少录音权限
**解决：** 进入系统设置授予录音权限

### Q3: 一直在"正在监听"
**原因：** 环境太安静或阈值太高
**解决：** 降低能量阈值或更靠近麦克风说话

### Q4: 录音立即结束
**原因：** 环境噪音太大或阈值太低
**解决：** 提高能量阈值或换到安静环境

### Q5: 指令执行失败
**原因：** 指令不在白名单或被黑名单拦截
**解决：** 查看日志确认具体原因

### Q6: API调用失败
**原因：** 网络问题或API Key无效
**解决：** 检查网络连接和API Key有效性

---

## 📊 性能指标

- **服务启动时间**：< 2秒
- **VAD响应时间**：实时（< 100ms）
- **录音处理延迟**：< 1秒
- **API调用时间**：2-5秒（取决于网络）
- **内存占用**：30-50 MB
- **电池消耗**：轻度（后台运行约 5-8% / 小时）

---

## 🔄 版本更新

当前版本：**v1.0**

**后续计划：**
- [ ] 唤醒词支持
- [ ] 离线语音识别
- [ ] 多语言支持
- [ ] 自定义指令映射
- [ ] 历史记录查看
- [ ] 云端配置同步

---

## 📧 技术支持

如遇到问题，请提供以下信息：
1. Android版本和设备型号
2. 完整的Logcat日志
3. 问题复现步骤
4. 应用版本号

**日志收集命令：**
```bash
adb logcat -d > kimivoice_log.txt
```

---

## 📄 开源协议

本项目基于 MIT License 开源

---

**祝您使用愉快！** 🎉
